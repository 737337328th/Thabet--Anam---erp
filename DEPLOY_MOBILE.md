# تشغيل V1.7 من الجوال

إذا كان الخادم السحابي جاهزًا، لا يحتاج المكتب إلى كمبيوتر دائمًا: الاستخدام اليومي يتم من متصفح Android.

## على الخادم
1. ارفع الحزمة إلى الخادم.
2. عدّل `.env` وضع أسرارًا قوية.
3. شغّل `docker compose up -d --build`.
4. لا تفتح منفذ PostgreSQL للعامة.
5. استخدم HTTPS عبر reverse proxy.

## النسخ الاحتياطي
شغّل:
`./scripts/backup_rotate.sh`

ثم يمكن جدولة الأمر بـ cron. احتفظ بنسخة ثانية خارج الخادم؛ النسخة الوحيدة على نفس الخادم ليست استراتيجية نسخ احتياطي كافية.

> لا توجد مفاتيح أو كلمات مرور حقيقية داخل هذه الحزمة.
