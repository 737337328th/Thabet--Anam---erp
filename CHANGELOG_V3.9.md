# ثابت أنعم ERP — V3.9

## بوابة الاستقرار والتحصين قبل V4
- رفع إصدار API إلى 3.9.0.
- إضافة سجل إصدار قاعدة البيانات (`schema_info`) للتحقق من توافق مخطط البيانات مع إصدار التطبيق.
- إضافة `/api/system/schema` للمدير للتحقق من حالة مخطط قاعدة البيانات.
- تشديد متطلبات `THABET_SECRET` في بيئة الإنتاج: سر فريد لا يقل عن 32 حرفًا.
- منع تشغيل الإنتاج باستخدام كلمة مرور المدير الافتراضية `admin123`.
- إصلاح إغلاق الفترة المالية عندما يكون صافي الربح/الخسارة صفرًا، بحيث لا يحدث وصول إلى `entry.id` عندما لا يوجد قيد إقفال.
- تحديث اختبارات الإصدارات السابقة لتطابق 3.9.0.
- إضافة اختبارات تحقق لمخطط البيانات وإغلاق السنة بصافي صفر.

## بوابة الإصدار
- compileall: ناجح.
- pytest: **15/15 ناجحة**.
- فحص ZIP: ناجح.

> V3.9 هي مرحلة تحصين ومراجعة قبل V4.0. لا تعني وحدها أن النظام جاهز للإنتاج قبل تنفيذ بوابة V4 الشاملة، بما فيها اختبار قاعدة بيانات PostgreSQL الفعلي، الهجرة الآمنة للمخطط، النسخ والاستعادة، والصلاحيات على كل مسارات العمليات.
